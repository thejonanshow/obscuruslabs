type Product {
  id: ID!
  name: String!
  brand: String!
  model: String!
  priceCents: Int!
  description: String!
  batteryLife: String!
}

type Query {
  primaryProduct: Product
}

type Mutation {
  createStripeSession(productId: ID!): String!
}
